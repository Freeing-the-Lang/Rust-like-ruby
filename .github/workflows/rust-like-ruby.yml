name: "🦀 Rust-like-Ruby — Auto Build, Zip & Release v1.3"

on:
  push:
    branches: [ main ]
  pull_request:
    types: [ closed ]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: write

concurrency:
  group: rust-like-ruby-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: "🧭 Checkout Repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: "🔧 Setup Ruby"
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      - name: "🏗️ Prepare Build Directory"
        shell: bash
        run: |
          mkdir -p dist
          cp -r src examples dist/

      - name: "🧪 Run Example Test"
        shell: bash
        run: |
          ruby src/rust_like_ruby.rb examples/hello.rsrb > dist/output.log
          echo "✅ Test execution complete."
          cat dist/output.log

      - name: "⚙️ Generate Executable"
        shell: bash
        run: |
          echo "📦 Creating executable..."
          echo '#!/usr/bin/env ruby' > dist/rust_like_ruby
          cat src/rust_like_ruby.rb >> dist/rust_like_ruby
          chmod +x dist/rust_like_ruby
          ls -lh dist/

      - name: "🧾 Generate ProofLedger"
        shell: bash
        run: |
          echo "🔐 Generating ProofLedger..."
          sha256sum dist/rust_like_ruby > dist/proofledger.txt || shasum -a 256 dist/rust_like_ruby > dist/proofledger.txt
          echo "Timestamp: $(date -u)" >> dist/proofledger.txt
          echo "Runner: ${{ runner.os }}" >> dist/proofledger.txt
          cat dist/proofledger.txt

      - name: "📦 Create ZIP Package"
        shell: bash
        run: |
          cd dist
          zip -r "Rust-like-Ruby-v1.3-${{ runner.os }}.zip" rust_like_ruby proofledger.txt examples output.log
          ls -lh *.zip

      - name: "💾 Commit ProofLedger (self-healing)"
        if: github.ref == 'refs/heads/main'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/proofledger.txt || true
          git commit -m "🔄 Auto-update ProofLedger [${{ runner.os }}]" || echo "No new changes"
          git push origin main || echo "Already up to date"

      - name: "🏷️ Auto Tag"
        if: github.ref == 'refs/heads/main'
        shell: bash
        run: |
          VERSION="v1.3.$(date +%Y%m%d%H%M)"
          echo "Creating tag: $VERSION"
          git tag -a "$VERSION" -m "Automated release $VERSION"
          git push origin "$VERSION"
          echo "TAG_NAME=$VERSION" >> $GITHUB_ENV

      - name: "🚀 Publish GitHub Release"
        if: always()
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: "Rust-like-Ruby ${{ env.TAG_NAME }}"
          body: |
            🦀 **Rust-like-Ruby v1.3 Auto Release**
            - OS: ${{ runner.os }}
            - Includes: executable + proofledger + example
            - Generated automatically by GitHub Actions
          files: |
            dist/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
